<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fistorias.android.pegadas" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:logo="@drawable/ic_logo"
        android:label="@string/app_name"

        android:theme="@style/PegadasTheme" >
        <activity
            android:name="com.fistorias.android.pegadas.MainActivity" android:label="@string/app_name" android:theme="@style/MainActivityTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.fistorias.android.pegadas.OptionListActivity" android:label="@string/app_name"   android:theme="@style/PegadasTheme">
        </activity>
    </application>

</manifest>
